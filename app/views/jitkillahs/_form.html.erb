<%= form_with(model: jitkillah) do |form| %>
  <% if jitkillah.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(jitkillah.errors.count, "error") %> prohibited this jitkillah from being saved:</h2>

      <ul>
        <% jitkillah.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
